# Core serving stack
ray[serve]>=2.9.0,<3.0.0
fastapi>=0.110.0
uvicorn>=0.27.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
starlette>=0.36.0

# Metrics / observability
prometheus-client>=0.19.0
requests>=2.31.0
pyyaml>=6.0.1

# Load testing / client utilities
numpy>=1.24.0
aiohttp>=3.9.0

# Optional real-model backends (install with: pip install -r requirements-ml.txt)
# torch>=2.1.0
# transformers>=4.38.0
# sentence-transformers>=2.5.0
